Feature: 單日行程規劃

  驗證輸入目標地點 → 推薦景點/美食 → 勾選 → 產生排序行程的完整流程。
  串接真實運行中的後端，後端呼叫真實 TDX 觀光資訊資料庫與真實 Google Maps API，不使用任何替身。
  斷言採結構性驗證（筆數、順序編號），不檢查具體地名，避免 TDX 資料異動造成測試脆弱。

  Scenario: 輸入座標後應能推薦景點並產生排序行程
    Given 使用者開啟首頁
    When 使用者輸入目標座標 "25.0478,121.5170" 並送出查詢
    Then 附近景點/美食推薦清單筆數應大於 0
    When 使用者勾選前 2 筆推薦景點並產生行程排序
    Then 行程結果應包含 2 筆站點
    And 行程結果的順序編號應為 1 到 2 依序遞增
